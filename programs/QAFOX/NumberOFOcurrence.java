package programs.QAFOX;

public class NumberOFOcurrence {
    public static void main(String[] args) {
        String str = "hello how are you ";
        char c[]=str.toCharArray();
        for (int i = 0; i < c.length; i++) {
            
        }
        // int beforelength = str.length();
        // String str2 = str.replaceAll("o", "");
        // int afterlength = str2.length();
        // System.out.println("the number of occurances of this word is :" +(beforelength-afterlength));
    }
    
}
