package DSA.Strings;

import java.util.Scanner;

public class Strings {
    /**
     * @param args
     */
    public static void main(String[] args) {
        // char arr[]= new char[5]; we can define string in this way as a character array 
        // String name  = "dhairya"; normal way to create and intialize string 
        // Scanner scanner = new Scanner(System.in); takes input as a string 
        // String str = scanner.nextLine();
            String str = "dhairya";
            // System.out.println(str.length()); to find the lenght of the string 
            // System.out.println(str.charAt(0)); to find the position of char in given string 
            // System.out.println(str.indexOf("d")); index of given character
            // System.out.println(str.contains("irya")); gives true if contains false if not 
            // toUpper and toLower 
            // System.out.println(str.concate(str2));
            // we can add string with char and integer 
            // System.out.println(str.substring(0, 4)); it will not include the last index
            // System.out.println(str.substring(0)); not include first and go upto last
            // here in the string == only checks the address of the string 
            // while Str.equals() checks the content of the string not the address
            // due to poor performance , immutablity , interning we use string builder 
            




    }
    
}
