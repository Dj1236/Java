package DSA.LinkedList.Problems;

public class Q3 {
    // we have to find the nth node(m-n+1 node from the start) from the end of the
    // linked list and only the head is given
    
    /*
     * public static void deleteNthNodeFromEnd(Node head){
     *     Node slow = head ;
     *     Node fast = head;
     *    for(int i =1;i<n;i++){
     *    fast=fast.next; 
     * }
     *   if (fast==null){
     *    head = head.next;
     *    return;
     * }
     * 
     *    while(fast.next != null){
     *     slow= slow.next;
     *     fast = fast.next;
     * }
     *   slow.next = slow.next.next;
     * }
     */
    
}
