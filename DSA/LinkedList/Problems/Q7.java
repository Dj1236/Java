package DSA.LinkedList.Problems;

public class Q7 {
    // to detect the cycle in the linked list
    /*
     * while(fast!=null ){
     * slow = slow.next;
     * fast = fast.next.next;
     * if(fast==slow) return true
     * return false;
     * }
     * 
     */
    
}
