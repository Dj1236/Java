package DSA.LinkedList.Problems;

public class Q2 {
    // we have to find the nth node(m-n+1 node from the start) from the end of the linked list and only the head is given
    /*
     * public static node nthNode(Node head){
     * 
     * int size = 0;
     * Node temp=head;
     * while(temp!=null){
     * size++;
     * temp=temp.next;
     * }
     * int m = size - n +1;
     * temp = head;
     * for(int i=1;i<=m-1;i++){
     * temp = temp.next;
     * }
     * return temp
     * 
     * 
     * }
     * 
     * public static node nthNode(Node head){
     *   Node slow = head;
     *   Node fast = head;
     *   for(int i=1;i<=n;i++){
     *   fast = fast.next;
     * }
     *  while(fast!=null){
     *   slow = slow.next;
     *  fast = fast.next;
     * 
     * } return slow;
     * }
     */
    
}
